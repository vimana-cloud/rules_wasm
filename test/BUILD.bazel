load("//:rust.bzl", "rust_component")

# Example to build a simple component with a custom interface.
rust_component(
    name = "foo",
    srcs = ["foo.rs"],
    wit = "foo.wit",
    world = "the-world",
)

# Example to build a wrapper component to run `foo`,
# using the standard `wasi:cli` interface.
rust_component(
    name = "foo-runner",
    srcs = ["runner.rs"],
    wit = "//wasip2:cli",
    world = "command",
)
